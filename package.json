{
  "name": "mailtrace-ai",
  "version": "1.0.0",
  "description": "MailTrace AI — Pre-Delivery Email Security Gateway + Webmail Interface",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:backend": "py -3.12 -m uvicorn backend.app.main:app --reload --port 8000",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run dev"
  }
}
